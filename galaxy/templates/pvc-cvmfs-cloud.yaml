{{- if .Values.cvmfs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "galaxy.fullname" . }}-cvmfs-gxy-cloud-pvc
  labels:
    {{- include "galaxy.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: {{ .Values.cvmfs.cloud.pvc.storage }}
  storageClassName: {{ .Values.cvmfs.cloud.pvc.storageClassName }}
{{- end }}
